import styled from 'styled-components';
import { Functions, Tokens } from '../../styles';

const { rem } = Functions;
const { Colors, TypeSpecs } = Tokens;

export const Styled__Component__ = styled.div`
  border: 10px solid red;
`;